{
  "name": "checkout-jenkins-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Small checkout API for a Jenkins CI/CD classroom demo",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "engines": { "node": ">=20" }
}
